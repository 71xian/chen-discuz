<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.chenyuxian.discuz.system.modular.thread.mapper.ThreadRedPacketsMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.chenyuxian.discuz.system.modular.thread.entity.ThreadRedPackets">
        <id column="id" property="id" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
        <result column="thread_id" property="threadId" />
        <result column="post_id" property="postId" />
        <result column="rule" property="rule" />
        <result column="condition" property="condition" />
        <result column="likenum" property="likenum" />
        <result column="money" property="money" />
        <result column="number" property="number" />
        <result column="remain_money" property="remainMoney" />
        <result column="remain_number" property="remainNumber" />
        <result column="status" property="status" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        created_at,
        updated_at,
        id, thread_id, post_id, rule, condition, likenum, money, number, remain_money, remain_number, status
    </sql>

</mapper>
